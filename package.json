{
  "name": "mpwa-socket",
  "version": "1.0.0",
  "private": true,
  "description": "Socket.io real-time relay for the WhatsApp CRM chat (Meta Cloud API). PHP pushes events to POST /push; browsers join conversation/inbox rooms.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "socket.io": "^4.7.5"
  }
}
